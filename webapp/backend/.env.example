# Copy this file to .env and fill in your values.
# Do not commit .env (it is in .gitignore).

# Optional: Python command for scripts (e.g. python3 or py -3)
# ECHOLOG_PYTHON=python3

# Optional: Whisper model when using local Whisper (tiny, base, small, medium, large-v2)
# WHISPER_MODEL=base

# ElevenLabs API key for transcription (tried first when set)
# When set, transcribe_audio.py uses ElevenLabs speech-to-text (scribe_v2).
# Get a key at https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=

# OpenAI API key for transcription (tried if ElevenLabs not set or fails)
# When set, transcribe_audio.py can use the OpenAI Whisper API.
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: JWT secret for auth
# JWT_SECRET=your-secret-key

# Optional: Use mock data (default true if not set)
# USE_MOCK_DATA=true

# Optional: Port for the backend
# PORT=3001
